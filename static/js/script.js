const input=document.getElementById("resume");const dropzone=document.getElementById("dropzone");const fileName=document.getElementById("fileName");if(input){input.addEventListener("change",e=>{if(e.target.files[0])fileName.textContent=e.target.files[0].name})}if(dropzone){["dragenter","dragover"].forEach(n=>dropzone.addEventListener(n,e=>{e.preventDefault();dropzone.style.borderColor="#8195ff"}));["dragleave","drop"].forEach(n=>dropzone.addEventListener(n,e=>{e.preventDefault();dropzone.style.borderColor=""}));dropzone.addEventListener("drop",e=>{const f=e.dataTransfer.files[0];if(f)fileName.textContent=f.name})}